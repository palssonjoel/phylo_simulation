################################################################################
03-09-202

I have implemented a properitary HKY model which I think works. I need to figure
out proper ways of testing this, as well as the realism of the transmission chain
simulation. I should probably tweak the simulation parameters, is there any way
of bootstrapping this?

Additionally, I should scale up the simluation to more countries, potentially to
encompass all of Europe. 

A thought: If I start the epidemic outside Denmark, it stands to reason that 
Denmark will be entirely absent from the transmission chain since it hardly has
any imports. This should be easy to test.

To do:
 - Structure scripts to work by running one initiating function call
 - Add log output with relevant warnings and timestamps
 - For HKY, entire sequences are saved. This may not scale well, consider saving only snp locations
 - Save output and implement checks - if files exist, don't run.